---
phase: 01-foundation-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components.json
  - components/ui/button.tsx
  - components/ui/card.tsx
  - components/ui/input.tsx
  - components/ui/dialog.tsx
  - tailwind.config.ts
autonomous: true

must_haves:
  truths:
    - "Next.js 16 project exists with App Router and TypeScript enabled"
    - "Tailwind CSS v4 is configured with CSS-first configuration in globals.css"
    - "shadcn/ui is initialized with base components (button, card, input, dialog) available"
    - "All NPM dependencies install without errors (Supabase SSR, AI SDK, Google APIs)"
    - "npm run dev starts the development server without errors on localhost:3000"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all dependencies"
      contains: "@supabase/ssr"
    - path: "app/layout.tsx"
      provides: "Root layout with TypeScript and metadata"
      contains: "export default function RootLayout"
    - path: "app/globals.css"
      provides: "Tailwind v4 CSS-first configuration with shadcn/ui theme variables"
      contains: "@import \"tailwindcss\""
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "\"style\":"
    - path: "components/ui/button.tsx"
      provides: "Base shadcn/ui Button component"
      contains: "buttonVariants"
  key_links:
    - from: "app/globals.css"
      to: "components/ui/*"
      via: "Tailwind v4 theme variables consumed by shadcn/ui components"
      pattern: "@theme inline"
    - from: "package.json"
      to: "node_modules"
      via: "npm install resolves all dependencies"
      pattern: "@supabase/ssr.*ai.*googleapis"
---

<objective>
Initialize Next.js 16 project with App Router, TypeScript, and Tailwind CSS v4. Install all project dependencies (Supabase SSR, Vercel AI SDK, Google APIs, shadcn/ui). Configure shadcn/ui component library with Tailwind v4 theme and add base components.

Purpose: Establishes the project foundation that all subsequent phases build upon. Without a working Next.js project with properly configured dependencies, no feature development can begin.

Output: A fully scaffolded Next.js 16 project with all dependencies installed, shadcn/ui configured with Tailwind v4, and base UI components ready for use.
</objective>

<execution_context>
@/Users/RAZER/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RAZER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-environment/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 16 project and install all dependencies</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    app/layout.tsx
    app/page.tsx
    app/globals.css
  </files>
  <action>
    IMPORTANT: The project root is `/Users/RAZER/Documents/projects/sales tracking`. The Next.js app must be created IN this directory (not as a subdirectory). Since the directory already has files (.planning/, .git/, etc.), use a temporary directory approach:

    1. Create the Next.js project in a temp directory, then move files:
       ```bash
       cd /tmp
       npx create-next-app@latest sales-tracking-init --yes
       ```
       The --yes flag selects all recommended defaults: TypeScript, Tailwind CSS, ESLint, App Router, Turbopack, @/* alias.

    2. Move all generated files (excluding .git) into the project root:
       ```bash
       # Copy everything from temp to project root
       cp -r /tmp/sales-tracking-init/* "/Users/RAZER/Documents/projects/sales tracking/"
       cp /tmp/sales-tracking-init/.eslintrc* "/Users/RAZER/Documents/projects/sales tracking/" 2>/dev/null
       cp /tmp/sales-tracking-init/.gitignore "/Users/RAZER/Documents/projects/sales tracking/" 2>/dev/null
       cp /tmp/sales-tracking-init/eslint.config.* "/Users/RAZER/Documents/projects/sales tracking/" 2>/dev/null
       rm -rf /tmp/sales-tracking-init
       ```

    3. Install all project dependencies from the project root:
       ```bash
       cd "/Users/RAZER/Documents/projects/sales tracking"
       npm install
       npm install @supabase/ssr @supabase/supabase-js
       npm install ai @ai-sdk/anthropic
       npm install googleapis
       npm install lucide-react next-themes
       ```

    4. Verify installation:
       ```bash
       npm ls @supabase/ssr @supabase/supabase-js ai @ai-sdk/anthropic googleapis lucide-react next-themes
       ```

    AVOID: Do NOT use `create-next-app` directly in the project root -- it will refuse because the directory is not empty. Do NOT delete .git or .planning directories.
  </action>
  <verify>
    - `ls package.json app/layout.tsx app/page.tsx tsconfig.json next.config.ts` shows all files exist
    - `npm ls @supabase/ssr` shows installed version
    - `npm ls ai` shows installed version
    - `npm ls googleapis` shows installed version
    - `npx tsc --noEmit` compiles without TypeScript errors
  </verify>
  <done>
    Next.js 16 project exists at project root with App Router, TypeScript, Tailwind CSS, and all dependencies installed. package.json contains @supabase/ssr, @supabase/supabase-js, ai, @ai-sdk/anthropic, googleapis, lucide-react, next-themes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and add base components</name>
  <files>
    components.json
    components/ui/button.tsx
    components/ui/card.tsx
    components/ui/input.tsx
    components/ui/dialog.tsx
    app/globals.css
    lib/utils.ts
  </files>
  <action>
    1. Initialize shadcn/ui from the project root:
       ```bash
       cd "/Users/RAZER/Documents/projects/sales tracking"
       npx shadcn@latest init --defaults
       ```
       The --defaults flag selects: New York style, Zinc base color, CSS variables enabled.

       This will:
       - Create components.json with shadcn/ui configuration
       - Update app/globals.css with Tailwind v4 theme variables (OKLCH colors, @theme inline block)
       - Create lib/utils.ts with the cn() utility function
       - Install tw-animate-css (replaces deprecated tailwindcss-animate for v4)

    2. Verify globals.css has Tailwind v4 CSS-first configuration:
       - Must contain `@import "tailwindcss"` (NOT @tailwind directives)
       - Must contain `@import "tw-animate-css"` (NOT tailwindcss-animate)
       - Must contain `@custom-variant dark` directive
       - Must contain `:root` with OKLCH color variables
       - Must contain `@theme inline` block mapping CSS vars to Tailwind

    3. Add base shadcn/ui components needed across the application:
       ```bash
       npx shadcn@latest add button card input dialog label separator
       ```
       These are the foundational components used in almost every phase.

    4. Verify a component renders correctly by updating app/page.tsx to use a shadcn/ui button:
       Replace the default Next.js landing page content with a minimal page that imports and renders a Button component. This validates the full chain: Tailwind v4 CSS -> shadcn theme -> component rendering.

       ```tsx
       // app/page.tsx
       import { Button } from "@/components/ui/button";

       export default function Home() {
         return (
           <main className="flex min-h-screen flex-col items-center justify-center p-24">
             <h1 className="text-4xl font-bold mb-8">Prytaneum Investor CRM</h1>
             <p className="text-muted-foreground mb-4">Foundation ready. Building pipeline.</p>
             <Button>Get Started</Button>
           </main>
         );
       }
       ```

    AVOID: Do NOT use tailwindcss-animate (v3 plugin, breaks v4). Use tw-animate-css instead. Do NOT create a tailwind.config.js file -- Tailwind v4 uses CSS-first config only.
  </action>
  <verify>
    - `cat components.json` shows valid shadcn/ui configuration with style and tailwind CSS settings
    - `ls components/ui/button.tsx components/ui/card.tsx components/ui/input.tsx` shows components exist
    - `grep "@import" app/globals.css` shows tailwindcss and tw-animate-css imports
    - `grep "@theme inline" app/globals.css` shows Tailwind v4 theme block exists
    - `npm run dev` starts without errors and localhost:3000 shows the placeholder page with styled button
  </verify>
  <done>
    shadcn/ui is initialized with Tailwind v4 CSS-first configuration. Base components (button, card, input, dialog, label, separator) are installed in components/ui/. The home page renders a styled shadcn/ui Button, confirming the full CSS pipeline works. lib/utils.ts provides the cn() utility.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts development server on localhost:3000 without errors
2. Visiting localhost:3000 shows "Prytaneum Investor CRM" heading with styled Button
3. `npx tsc --noEmit` reports no TypeScript errors
4. `npm ls @supabase/ssr ai googleapis` confirms all dependencies installed
5. `cat components.json` shows valid shadcn/ui config
6. `grep -c "@import" app/globals.css` shows Tailwind v4 imports present
</verification>

<success_criteria>
- Next.js 16 project scaffolded with TypeScript, App Router, and Tailwind CSS v4
- All dependencies installed: @supabase/ssr, @supabase/supabase-js, ai, @ai-sdk/anthropic, googleapis, lucide-react, next-themes
- shadcn/ui initialized with Tailwind v4 theme and 6 base components
- Development server runs without errors on localhost:3000
- Page renders shadcn/ui components correctly with Tailwind styling
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-environment/01-01-SUMMARY.md`
</output>
